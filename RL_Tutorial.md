# Reinforcement Learning 入门教程

> 视觉学习者友好版 🔥  
> 混合中英文，图多字少

---

## 第一章：RL 是什么？

### 核心循环

```
┌─────────────────────────────────────────────────┐
│                                                 │
│   AGENT  ──── action ────▶  ENVIRONMENT         │
│     ▲                           │               │
│     │                           │               │
│     └────── reward + state ─────┘               │
│                                                 │
└─────────────────────────────────────────────────┘
```

Agent 活在一个循环里：
1. **See** — 观察当前状态 (state)
2. **Do** — 执行动作 (action)
3. **Get** — 收到奖励 + 新状态 (reward + new state)
4. **Repeat** — 不断循环，慢慢变聪明

---

### 具体例子：格子迷宫 🐭

```
┌───┬───┬───┬───┐
│ 🐭 │   │   │   │
├───┼───┼───┼───┤
│   │ ■ │   │   │   ■ = 墙（不能走）
├───┼───┼───┼───┤
│   │   │   │ 🧀 │   🧀 = 奖励 (+1)
└───┴───┴───┴───┘
```

| 概念 | 定义 | 迷宫例子 |
|---|---|---|
| **State (s)** | Agent 当前观察到的情况 | 鼠标所在格子 (row, col) |
| **Action (a)** | Agent 可以执行的操作 | ↑ ↓ ← → |
| **Reward (r)** | 环境给的反馈信号 | 到达🧀得 +1，其他为 0 |

---

### 为什么用 RL 而不是写规则？

```
传统编程：                    强化学习：

规则 ──▶ 电脑 ──▶ 输出        经验 ──▶ Agent ──▶ 规则
```

RL 适合**规则难以手写**的场景 — 机器人走路、下棋、打游戏。

---

## 第二章：Policy（策略）π

### 什么是 Policy？

Policy 就是 agent 的**决策手册**：给定状态，输出动作。

```
State ──▶ Policy (π) ──▶ Action

  🐭 at (0,0)  ──▶  π  ──▶  →
  🐭 at (0,1)  ──▶  π  ──▶  ↓
  🐭 at (1,0)  ──▶  π  ──▶  →
```

希腊字母 **π (pi)** = policy，RL 论文里随处可见。

---

### 两种 Policy

```
┌─────────────────────────────────────────────────────┐
│  DETERMINISTIC（确定性）  │  STOCHASTIC（随机性）     │
│                           │                          │
│  状态 → 唯一动作           │  状态 → 动作概率分布       │
│                           │                          │
│  (0,0) → 永远走 →         │  (0,0) → 70% →           │
│                           │          20% ↓           │
│  像 GPS 导航               │          10% ↑           │
│                           │                          │
│                           │  像考试乱蒙的学生 😅       │
└─────────────────────────────────────────────────────┘
```

- 训练早期 → **Stochastic**：随机探索，啥都试
- 训练成熟 → **Deterministic**：自信果断，直奔目标

---

### RL 的终极目标（一句话）

> **找到策略 π，使得累计 reward 最大化。**

其他所有算法，都是在解决"怎么找到这个 π"的问题。

---

## 第三章：Value Function（价值函数）

### 1. 为什么需要 Value Function？

先看问题：只靠 Reward 能指导 Agent 吗？

```
只看眼前 Reward:        看长远 Value:

┌───┬───┬───┐          ┌────┬────┬────┐
│ 0 │ 0 │ 0 │          │0.73│0.81│0.90│
├───┼───┼───┤    vs    ├────┼────┼────┤
│ 0 │ 0 │ 0 │          │0.50│ ■  │1.00│
├───┼───┼───┤          ├────┼────┼────┤
│ 0 │ 0 │ 🧀│          │0.30│0.50│ 🧀 │
└───┴───┴───┘          └────┴────┴────┘

只看眼前 → 全是0，懵逼    看长远 → 知道往哪走 ✅
```

**问题**：Reward 只在终点有值，其他地方全是 0，Agent 根本不知道方向对不对。

**解法**：Value Function — 把终点奖励"向外传播"，让每一步都有方向感。

---

### 2. Reward vs Value — 核心区别

```
┌────────────────────────────────────────────────────────┐
│                                                        │
│  REWARD r(s,a)              VALUE V(s)                 │
│  ──────────────             ───────────                │
│                                                        │
│  "我现在拿到多少？"            "从这里出发，总共能拿多少？"  │
│                                                        │
│  即时的、局部的                长远的、全局的              │
│  环境给的（外部）               Agent 学出来的（内部）      │
│  固定不变                      通过经验不断更新            │
│                                                        │
└────────────────────────────────────────────────────────┘
```

**类比：**
```
Reward  = 考试成绩（只有考完才知道）
Value   = 平时估分（提前预测能考多少）
Policy  = 学习计划（根据估分调整策略）
```

| | Reward `r` | Value `V(s)` |
|---|---|---|
| **是什么** | 环境给的即时信号 | Agent 算的长远期望 |
| **谁给的** | 环境（外部） | Agent 自己学习（内部） |
| **时间跨度** | 当前这一步 | 从现在到未来所有步 |
| **用途** | 反馈信号（输入） | 指导决策（中间产物） |

---

### 3. 在 RL 整体中的定位

```
┌──────────────────────────────────────────────────────────┐
│                      RL 学习循环                          │
│                                                          │
│  ┌─────────────┐  Reward r   ┌──────────────────────┐   │
│  │ ENVIRONMENT │ ──────────▶ │        AGENT         │   │
│  │             │             │                      │   │
│  │ ・状态空间    │  State s    │ ・Value Function V(s) │   │
│  │ ・动作空间    │ ──────────▶ │   （要学习）          │   │
│  │ ・Reward规则  │             │                      │   │
│  │  （固定的）  │  Action a   │ ・Policy π(s)         │   │
│  │             │ ◀────────── │   （要学习）          │   │
│  └─────────────┘             └──────────────────────┘   │
│                                                          │
└──────────────────────────────────────────────────────────┘

流程：
1. Environment 给 Reward（即时反馈，环境定义，不可改）
2. Agent 用 Reward 学习 Value Function（长远估计，从经验中来）
3. Value Function 指导 Policy（决策）
4. Policy 输出 Action（执行）
```

**关键点：**
- **Reward** 属于环境，是外部输入，Agent 无法改变
- **Value** 属于 Agent，是内部估算，需要学习
- **Reward 是原料，Value 是 Agent 加工出来的地图**

---

### 4. V(s) 的公式

```
V(s) = r₁ + γ·r₂ + γ²·r₃ + γ³·r₄ + ...

时间线：  现在    +1步    +2步    +3步
reward：   r₁  +   r₂  +   r₃  +   r₄
折扣后：   r₁  + γ·r₂ + γ²·r₃ + γ³·r₄
                  ↑       ↑       ↑
               9折      81折    73折（γ=0.9时）
```

**γ (gamma) = 折扣因子**，取值 0 到 1：
```
γ = 0.9  →  很在乎未来，目光长远 🔭
γ = 0.1  →  只顾眼前，及时行乐 🎯
```

**计算例子：**
```
路径： (0,0) → (0,1) → (0,2) → 🧀，γ = 0.9
奖励：   0       0       0      +1

V(0,0) = 0 + 0 + 0 + 0.9³×1 = 0.729

走弯路多一步：
V(弯路) = 0.9⁴×1 = 0.656  ← 比直路小

所以 Agent 自然学会走捷径 ✅
```

---

### 5. V(s) vs Q(s, a)

```
┌──────────────────────────────────────────────────────┐
│  V(s)                      │  Q(s, a)                │
│  State Value Function      │  Action Value Function  │
│                            │                         │
│  "这个状态值多少？"           │  "这个状态+这个动作值多少？"│
│                            │                         │
│  只看状态                   │  看状态 + 动作            │
│                            │                         │
│  V(🐭在(0,0)) = 0.73       │ Q(🐭在(0,0), →) = 0.73  │
│                            │ Q(🐭在(0,0), ↓) = 0.50  │
│                            │ Q(🐭在(0,0), ←) = 0.10  │
│                            │                         │
│  → 选动作还需额外计算         │ → 直接取最大值就能选动作 ✅ │
└──────────────────────────────────────────────────────┘
```

**Q(s,a) 更实用** — 直接告诉 Agent 选哪个动作，Q-Learning 就是用来学 Q(s,a) 的算法。

---

### 6. Bellman 方程 — Value 的递推核心

```
V(s) = max_a [ r(s,a) + γ × V(s') ]
               ↑              ↑
          当前reward      下一状态的value（打折）
```

Value 像波纹一样从奖励点向外扩散：

```
┌──────┬──────┬──────┬──────┐
│0.729 │ 0.81 │ 0.90 │ 🧀   │
│      │      │      │ 1.0  │
└──────┴──────┴──────┴──────┘

V(左3) = 0.9 × V(左2) = 0.9 × 0.81 = 0.729
V(左2) = 0.9 × V(左1) = 0.9 × 0.90 = 0.810
V(左1) = 0.9 × V(🧀)  = 0.9 × 1.0  = 0.900
```

跟着梯度走，Agent 自然找到路。

---

### 7. Value 和 Q-Learning 的关系

**一句话：Value Function 是目标（地图），Q-Learning 是画地图的过程（算法）。**

```
┌─────────────────────────────────────────────────────┐
│   Value Function（概念层）                            │
│   "我们想知道每个状态/动作值多少"                        │
│                                                     │
│   ┌───────────────────────────────────────────┐     │
│   │  Q-Learning（方法层）                      │     │
│   │  "一种具体学出 Q(s,a) 的算法"               │     │
│   └───────────────────────────────────────────┘     │
│                                                     │
│   其他方法：SARSA / DQN / Actor-Critic ...            │
└─────────────────────────────────────────────────────┘
```

在代码 `03_q_learning.py` 里：
```python
# Q表 = Q(s,a) 的具体存储形式
q_table = np.zeros([NUM_BINS] * 4 + [2])

# 更新 = Bellman 方程的实现
target = reward + GAMMA * np.max(q_table[next_state])
#         ↑                ↑
#     即时Reward        未来最大Value（打折）

q_table[state][action] += ALPHA * (target - q_table[state][action])
```

**Reward 是环境给的输入，Value 是 Agent 学出来的输出。**

---

## 路线图

```
✅ 第一章：RL 是什么（Agent / Environment / State / Action / Reward）
✅ 第二章：Policy π（确定性 vs 随机性）
✅ 第三章：Value Function V(s) 和 Q(s,a)，Bellman 方程
⬜ 第四章：Q-Learning — 自动学出 Q 表的算法
⬜ 第五章：Deep Q-Network (DQN) — 用神经网络替代 Q 表
⬜ 第六章：Policy Gradient — 直接优化策略
⬜ 第七章：Actor-Critic — 结合 Policy 和 Value
```

---

*生成时间：2026-03-11 | 持续更新中 🔥*
